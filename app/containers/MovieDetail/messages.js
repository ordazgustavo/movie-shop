/*
 * MovieDetail Messages
 *
 * This contains all the text for the MovieDetail container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.MovieDetail'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MovieDetail container!',
  },
})
